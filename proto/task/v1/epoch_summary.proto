syntax = "proto3";
package task.v1;

import "gogoproto/gogo.proto";
import "shared/v1/payload.proto";
import "shared/v1/rest_encoding.proto";

option go_package = "github.com/TrueOpen/node/x/task/types";

// EpochTaskSummaryCursorState is the bounded, restart-safe fold over finalized
// task summaries whose settlement height belongs to one epoch.
// EpochTaskSummaryCursorState defines the EpochTaskSummaryCursorState wire type.
message EpochTaskSummaryCursorState {
  uint64 epoch = 1;
  uint64 start_height = 2;
  uint64 end_height = 3;
  uint64 due_height = 4;
  uint64 last_settlement_height = 5;
  bytes last_task_id = 6 [(shared.v1.rest_bytes_encoding) = REST_BYTES_ENCODING_HASH32_LOWER_HEX];
  uint64 task_count = 7;
  uint64 valid_task_count = 8;
  repeated uint64 histogram = 9;
  // The runner retains the first bounded set of distinct models encountered in
  // canonical source order. The retained identifiers are sorted into strict
  // ascending UTF-8 byte order before receipt construction. Composite
  // model/profile strings are invalid.
  repeated string support_candidates = 10;
  // Number of valid-task candidate occurrences, including duplicates.
  uint64 support_candidate_seen_count = 11;
  bytes running_root = 12 [(shared.v1.rest_bytes_encoding) = REST_BYTES_ENCODING_HASH32_LOWER_HEX];
  uint64 visited_count = 13;
  // True once either configured bound causes a distinct model to be skipped.
  // Once set, it remains true for the rest of the fold.
  bool support_candidates_truncated = 14;
}

// EpochTaskSummaryReceiptState prevents replay from dispatching an epoch twice
// and retains the bounded payload plus its complete source commitment.
// EpochTaskSummaryReceiptState defines the EpochTaskSummaryReceiptState wire type.
message EpochTaskSummaryReceiptState {
  uint64 epoch = 1;
  uint64 start_height = 2;
  uint64 end_height = 3;
  uint64 due_height = 4;
  uint64 dispatched_height = 5;
  shared.v1.EpochTaskSummary summary = 6 [(gogoproto.nullable) = false];
  uint64 source_count = 7;
  // Number of valid-task candidate occurrences, including duplicates.
  uint64 support_candidate_seen_count = 8;
  uint32 retained_support_candidate_count = 9;
  bytes source_root = 10 [(shared.v1.rest_bytes_encoding) = REST_BYTES_ENCODING_HASH32_LOWER_HEX];
  bytes receipt_hash = 11 [(shared.v1.rest_bytes_encoding) = REST_BYTES_ENCODING_HASH32_LOWER_HEX];
  // Commits whether either configured bound skipped a distinct model.
  bool support_candidates_truncated = 12;
}
